<div id="camposdinamicos">
  <%= f.simple_fields_for :respuestafor, f.object.respuestafor.order(:id) do |r| %>
    <div class="panel panel-default">
      <div id="accordion">
        <div class="card">
          <div class="card-header" id="<%= "encabezado" + r.object.formulario.id.to_s %>" >
            <h5 class="mb-0">
              <a class="btn btn-link collapsed" data-toggle="collapse" data-target="<%= "#collapse" + r.object.formulario.id.to_s %>" aria-expanded= "false" aria-controls="<%= "collapse" + r.object.formulario.id.to_s %>">
                <%= r.object.formulario.nombre %>
              </a>
            </h5>
          </div>
          <div id="<%= "collapse" + r.object.formulario.id.to_s %>" class="collapse" aria-labelledby="<%= "encabezado" + r.object.formulario.id.to_s %>" data-parent="#accordion">
            <div class="card-body">
                <%= render partial: 'mr519_gen/encuestasusuario/campo_valorcampo', 
                  locals: {f: r} 
                %>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
